name: manual_mode
description: Manual Mode showcase. Pure Dart, proves the core runs without Flutter.
publish_to: none
version: 0.1.0

environment:
  sdk: ^3.10.0


dependencies:
  cobalt: ^0.3.0

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.0
