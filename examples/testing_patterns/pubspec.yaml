name: testing_patterns
description: How to test an app built on Cobalt — overriding dependencies and the traps.
publish_to: none
version: 0.1.0

environment:
  sdk: ^3.10.0
  flutter: ">=3.38.0"


dependencies:
  cobalt: ^0.2.0
  cobalt_flutter: ^0.2.0
  flutter:
    sdk: flutter

dev_dependencies:
  lints: ^6.0.0
  cobalt_test: ^0.2.0
  flutter_test:
    sdk: flutter
