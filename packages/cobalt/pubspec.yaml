name: cobalt
description: Runtime core of Cobalt, a dependency injection framework with hierarchical scopes, deterministic disposal and async init graphs.
version: 0.1.2
repository: https://github.com/rutikeyone/cobalt
issue_tracker: https://github.com/rutikeyone/cobalt/issues
topics:
  - dependency-injection
  - di
  - service-locator
  - scopes

environment:
  sdk: ^3.10.0

resolution: workspace

dependencies:
  cobalt_annotations: ^0.1.0
  collection: ^1.19.0
  meta: ^1.16.0

dev_dependencies:
  cobalt_test: ^0.1.0
  leak_tracker: ^11.0.0
  test: ^1.25.0
