name: cobalt_test_flutter
description: Widget-test helpers for Cobalt — settle a graph that is still starting, and reach the root scope of a mounted application.
version: 0.3.0
repository: https://github.com/rutikeyone/cobalt
issue_tracker: https://github.com/rutikeyone/cobalt/issues
topics:
  - dependency-injection
  - di
  - testing
  - flutter

environment:
  sdk: ^3.10.0
  flutter: ">=3.38.0"


dependencies:
  cobalt_flutter: ^0.3.0
  flutter:
    sdk: flutter
  flutter_test:
    sdk: flutter

dev_dependencies:
  lints: ^6.0.0
  cobalt_test: ^0.3.0
