name: cobalt_logging
description: Cobalt adapter for package:logging — sends the DI graph's events to a dart.dev Logger.
version: 0.1.2
repository: https://github.com/rutikeyone/cobalt
issue_tracker: https://github.com/rutikeyone/cobalt/issues
topics:
  - dependency-injection
  - di
  - logging
  - observability

environment:
  sdk: ^3.10.0

resolution: workspace

dependencies:
  cobalt: ^0.1.0
  logging: ^1.3.0

dev_dependencies:
  cobalt_test: ^0.1.0
  test: ^1.25.0
