name: cobalt_logger
description: Cobalt adapter for package:logger — sends the DI graph's events to a Logger with its pretty console output.
version: 0.3.0
repository: https://github.com/rutikeyone/cobalt
issue_tracker: https://github.com/rutikeyone/cobalt/issues
topics:
  - dependency-injection
  - di
  - logging
  - observability

environment:
  sdk: ^3.10.0


dependencies:
  cobalt: ^0.3.0
  logger: ^2.7.0

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.0
