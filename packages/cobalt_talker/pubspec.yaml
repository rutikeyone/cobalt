name: cobalt_talker
description: Cobalt adapter for talker — reports the DI graph's events as talker logs, each kind with its own title and colour.
version: 0.1.1
repository: https://github.com/rutikeyone/cobalt
issue_tracker: https://github.com/rutikeyone/cobalt/issues
topics:
  - dependency-injection
  - di
  - logging
  - observability

environment:
  sdk: ^3.10.0

resolution: workspace

dependencies:
  cobalt: ^0.1.0
  talker: ^5.1.0

dev_dependencies:
  cobalt_test: ^0.1.0
  test: ^1.25.0
