# Dart / pub
.dart_tool/
.packages
pubspec.lock
coverage/
build/
doc/api/

# Flutter tooling — each generated example also carries its own .gitignore,
# but counter_codegen and the compat stand do not, so cover it here too.
.flutter-plugins
.flutter-plugins-dependencies
**/generated_plugin_registrant.dart
**/android/local.properties
**/android/.gradle/
**/ios/Flutter/Generated.xcconfig
**/ios/Flutter/flutter_export_environment.sh
**/ios/Flutter/ephemeral/
**/ios/Pods/
**/ios/.symlinks/

# Editors
*.iml
.idea/
.vscode/
*.xcuserdatad/
xcuserdata/

# macOS
.DS_Store
# Widget Preview related
.widget_preview/

# Design canvases. Scratch for how a screen should look, not a source the build
# reads — and the published export is two megabytes of HTML, which is enough to
# make GitHub call this an HTML project.
.design/

# LinkedIn cards for release posts. Made for social media, not read by any
# package, README or build.
assets/linkedin-card*
